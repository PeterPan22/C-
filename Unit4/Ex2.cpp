/*จงเขียนโปรแกรมด้วยคําสั่ง while, do-while, หรือ for สําหรับการคํานวณผลบวกของเลขอนุกรมต่อไปนี้
คือ 10 + 15 + 20 + 25 + …+ N เมื่อ N เป็นค่าที่รับจากคีย์บอร์ด */
#include <iostream>
using namespace std;
int main()
{
    int i,n,sum = 0;
    cout << "Enter number :";
    cin >> n;

    while(i<=n);
     sum +=i;
        (i%5==0);
        i++;

    cout << i << " is series number";
    cout << n << " is result series number" << endl;
    
    return(0);



}